######################## libsquish Config  ########################
# Generated by ob_standard_project_package_config()

####### Expanded from @PACKAGE_INIT@ by configure_package_config_file() #######
####### Any changes to this file will be overwritten by the next CMake run ####
####### The input file was __standard_primary_pkg_cfg.cmake.in                            ########

get_filename_component(PACKAGE_PREFIX_DIR "${CMAKE_CURRENT_LIST_DIR}/../" ABSOLUTE)

macro(check_required_components _NAME)
  foreach(comp ${${_NAME}_FIND_COMPONENTS})
    if(NOT ${_NAME}_${comp}_FOUND)
      if(${_NAME}_FIND_REQUIRED_${comp})
        set(${_NAME}_FOUND FALSE)
      endif()
    endif()
  endforeach()
endmacro()

####################################################################################

# Start by assuming package is found
set(libsquish_FOUND TRUE)

# Import target configs
set(__inclusion "${CMAKE_CURRENT_LIST_DIR}/Squish/libsquishSquishConfig.cmake")
if(NOT EXISTS "${__inclusion}")
   message(FATAL_ERROR "A default inclusion of libsquish was missing!")
elseif(Squish IN_LIST libsquish_FIND_COMPONENTS OR NOT libsquish_FIND_COMPONENTS)
   set(libsquish_Squish_FOUND TRUE)
   include("${__inclusion}")
endif()

 # Ensure all requested components were found
check_required_components(libsquish)
